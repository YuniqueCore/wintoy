<UserControl x:Class="FuturesTrader.Presentation.Controls.StateToggleButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             x:Name="Root">
    <UserControl.Resources>
        <!-- 状态感知 ToggleButton 样式：OFF=透明 / Hover=浅灰 / Pressed=深灰 / ON=Accent 填充+白字。
             与 SegmentToggleStyle 视觉一致（共享状态机），但宽度/高度针对图标按钮（系统工具栏）优化。
             FocusVisualStyle={x:Null} 移除 WPF 默认的红色焦点虚线框，对齐用户「switcher 不能有红框」约束。 -->
        <Style x:Key="InnerStateToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="400"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource ControlFillColorTertiaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource AccentFillColorDefaultBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ToggleButton x:Name="InnerToggle"
                  IsChecked="{Binding IsChecked, ElementName=Root, Mode=TwoWay}"
                  Command="{Binding Command, ElementName=Root}"
                  CommandParameter="{Binding CommandParameter, ElementName=Root}"
                  ToolTip="{Binding ToolTip, ElementName=Root}"
                  Style="{StaticResource InnerStateToggleButtonStyle}">
        <!-- Icon DP 内容展示：用 ContentControl 包装，保持 SymbolIcon 渲染原貌 -->
        <ContentControl Content="{Binding Icon, ElementName=Root}"
                        Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                        Focusable="False"/>
    </ToggleButton>
</UserControl>
